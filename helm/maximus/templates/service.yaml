apiVersion: v1
kind: Service
metadata:
  name: {{ include "maximus.fullname" . }}
  labels:
    {{- include "maximus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "maximus.selectorLabels" . | nindent 4 }}
